import styled from "styled-components"
import { backvasco } from "../../assets/vascoback.png"
export const Main = styled.main`
display:flex;
  background: ${backvasco};
  min-height: calc(100vh - 9rem);
`
