import styled from "styled-components"
import backvasco from "../../assets/vascoback.png"
export const Main = styled.main`
display:flex;
  background-image: url(${backvasco});
  min-height: calc(100vh - 12rem);
  background-size: 100%;
  align-items: center;
    justify-content: center;
`
